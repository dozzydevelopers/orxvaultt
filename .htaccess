RewriteEngine On

# Serve existing files/directories
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Let /api/* be handled by /api/.htaccess
RewriteRule ^api/ - [L]

# SPA fallback to index.html
RewriteRule . /index.html [L]
